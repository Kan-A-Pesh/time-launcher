document.onload = () => {
    let navbar = document.querySelector("#navbar");

    if (navbar == null)
    {
        console.log("No navbar found");
    }
    else
    {
        // TODO Import navbar from a file to the DOM
    }
}